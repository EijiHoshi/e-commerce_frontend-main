import { NgModule } from '@angular/core';
import { CheckoutPage } from './checkout.page';
import { FormsModule } from '@angular/forms';

@NgModule({
    imports: [FormsModule, CheckoutPage],
})
export class CheckoutPageModule { } 